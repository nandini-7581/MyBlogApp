<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete a Post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body class="update-page">
    <main class="container py-4 py-md-5">
        <header class="d-flex flex-wrap justify-content-between align-items-center gap-3 pb-3 mb-5 border-bottom">
            <a href="/" class="blog-header-logo text-body-emphasis text-decoration-none">Nandini's Blog</a>
            <nav class="d-flex gap-2" aria-label="Post navigation">
                <a href="/viewposts" class="btn btn-outline-secondary">View posts</a>
                <a href="/newpost" class="btn btn-primary">New post</a>
            </nav>
        </header>

        <section class="update-panel mx-auto">
            <div class="mb-4">
                <p class="text-uppercase text-danger fw-semibold small mb-2">Manage your writing</p>
                <h1 class="display-6 fw-bold mb-2">Delete a post</h1>
                <p class="text-body-secondary mb-0">Choose the post you want to permanently remove.</p>
            </div>

            <% if (posts.length === 0) { %>
                <div class="alert alert-info mb-0">There are no posts available to delete.</div>
            <% } else { %>
                <form action="/delete" method="POST">
                    <div class="mb-4">
                        <label for="postId" class="form-label fw-semibold">Post to delete</label>
                        <select id="postId" name="postId" class="form-select form-select-lg" required>
                            <% posts.forEach((post) => { %>
                                <option value="<%= post.id %>"><%= post.title %></option>
                            <% }) %>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-danger btn-lg">Delete post</button>
                    <a href="/viewposts" class="btn btn-outline-secondary btn-lg">Cancel</a>
                </form>
            <% } %>
        </section>
    </main>

    <footer class="py-4 text-center text-body-secondary border-top mt-5">
        <p class="mb-0">Nandini's Blog &copy; 2026</p>
    </footer>
</body>
</html>
